; The game modes' own routines, one label and incsrc per mode that has any,
; named by the lines in gamemode-code.asm beside this file. Entered with AXY
; 8-bit and the data bank pointed at this bank, and each returns with RTL.
;
; The editor regenerates this file from the project's own game mode files.
; It is shipped empty because the shipped fragment names no mode.
