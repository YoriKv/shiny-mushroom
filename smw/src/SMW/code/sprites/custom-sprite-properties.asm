; Each custom sprite's properties: a block of named defines per sprite --
; the acts-like number, the six Tweaker bytes in the vocabulary
; sprites/SpritePropertiesTemplate.asm sets, and the two extra property
; bytes -- and one %SMW_CustomSpriteProperties line binding the block to
; its number. A number with no block acts like the unused sprite, $36,
; with every property clear.
;
; The editor regenerates this file from each sprite's metadata sibling.
; It is shipped empty because there are none.
