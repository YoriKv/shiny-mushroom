; The custom sprites' own routines, one label and incsrc per sprite file,
; named by the rows in custom-sprites.asm beside this file. Entered with
; AXY 8-bit, X the sprite slot and the data bank pointed at this bank,
; and each returns with RTL.
;
; The editor regenerates this file from the project's own sprite files.
; It is shipped empty because the shipped rows are all zero.
