; The global and status bar routines, named by the defines in
; global-code.asm beside this file. Entered with AXY 8-bit and the data bank
; pointed at this bank, and each returns with RTS -- not RTL, which is the
; convention that tag has in the tool this follows.
;
; The editor regenerates this file from the project's own files. It is
; shipped empty because the fragment beside it names nothing.
