; The levels' own routines, one label and incsrc per level that has any,
; named by the rows in level-code.asm beside this file. Entered with AXY
; 8-bit and the data bank pointed at this bank, and each returns with RTL.
;
; The editor regenerates this file from the project's own code files. It is
; shipped empty because the shipped rows are all zero.
