; The sprite library a custom sprite may call into: one namespace and
; incsrc per file in code/sprites/lib/ -- the level code's own mechanism,
; so a file's labels are reached as filename_label, a library file may
; call another's, and every file is assembled whether anything calls it
; or not.
;
; The editor regenerates this file from the project's own library files.
; It is shipped empty because there are none.
